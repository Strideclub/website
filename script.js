const toggle=document.querySelector('.nav-toggle');const links=document.querySelector('.nav-links');const year=document.querySelector('#year');if(year)year.textContent=new Date().getFullYear();if(toggle&&links){toggle.addEventListener('click',()=>{const isOpen=links.classList.toggle('open');toggle.setAttribute('aria-expanded',isOpen?'true':'false')});links.querySelectorAll('a').forEach(link=>{link.addEventListener('click',()=>{links.classList.remove('open');toggle.setAttribute('aria-expanded','false')})})}const reveals=document.querySelectorAll('.reveal');const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('active');observer.unobserve(entry.target)}})},{threshold:.12});reveals.forEach(element=>observer.observe(element));

const galleryCarousel=document.querySelector('.gallery-carousel');if(galleryCarousel){const track=galleryCarousel.querySelector('.gallery-track');const slides=Array.from(track.children);const dotsWrap=galleryCarousel.querySelector('.gallery-dots');const prevBtn=galleryCarousel.querySelector('.gallery-prev');const nextBtn=galleryCarousel.querySelector('.gallery-next');const dots=slides.map((slide,index)=>{const dot=document.createElement('button');dot.type='button';dot.setAttribute('aria-label','Go to photo '+(index+1));dot.addEventListener('click',()=>{slide.scrollIntoView({behavior:'smooth',inline:'center',block:'nearest'})});dotsWrap.appendChild(dot);return dot});const setActive=index=>{dots.forEach((dot,dotIndex)=>dot.classList.toggle('active',dotIndex===index))};const slideObserver=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting)setActive(slides.indexOf(entry.target))})},{root:track,threshold:.6});slides.forEach(slide=>slideObserver.observe(slide));setActive(0);const scrollByAmount=()=>slides[0].getBoundingClientRect().width+16;if(prevBtn)prevBtn.addEventListener('click',()=>track.scrollBy({left:-scrollByAmount(),behavior:'smooth'}));if(nextBtn)nextBtn.addEventListener('click',()=>track.scrollBy({left:scrollByAmount(),behavior:'smooth'}))}
